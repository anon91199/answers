import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        HashSet<String> set = new HashSet<>();
        HashSet<String> printed = new HashSet<>();

        while (sc.hasNext()) {
            String s = sc.next();
            if (!set.add(s) && printed.add(s)) {
                System.out.println(s);
            }
        }
        sc.close();
    }
}
